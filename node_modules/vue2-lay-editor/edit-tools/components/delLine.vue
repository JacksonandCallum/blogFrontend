<template>
  <div class="del-line">
    <div
      @mousedown.prevent
      @click="execCommand()"
      :class="['head', { 'head-active': isChecked }]"
    >
      <!-- 删除线 -->
      <i class="iconfont icon-fuwenbenbianjiqi_zhonghuaxian"></i>
    </div>
  </div>
</template>

<script>
export default {
  name: "WorkspaceJsonAlignMode",
  components: {},
  props: {
    state: {
      default: [],
    },
  },
  data() {
    return {
      command: "strikeThrough",
    };
  },
  computed: {
    isChecked() {
      return this.state.includes(this.command);
    },
  },
  mounted() {},

  methods: {
    execCommand() {
      document.execCommand(this.command, false, null);
      this.$emit("click", this.command);
    },
  },
};
</script>

<style lang="scss" scoped>
.del-line .head {
  width: 30px;
  height: 30px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.del-line .head:hover {
  background: #f5f5f5;
}

.del-line .head-active {
  background: #eee;
}
</style>
