<template>
  <div class="default-text">
    <rich-text
      @change="onHandler"
      @commandState="onCommandState"
      v-if="themeType == 'text'"
    ></rich-text>
    <quote-temp
      @change="onHandler"
      @commandState="onCommandState"
      v-if="themeType == 'quote'"
    ></quote-temp>
    <state-temp
      @change="onHandler"
      @commandState="onCommandState"
      v-if="themeType == 'state'"
    ></state-temp>
    <colorblock-temp
      @change="onHandler"
      @commandState="onCommandState"
      v-if="themeType == 'colorblock'"
    ></colorblock-temp>
  </div>
</template>

<script>
import richText from "./components/richText.vue";
import quoteTemp from "./components/quoteTemp.vue";
import stateTemp from "./components/stateTemp.vue";
import colorblockTemp from "./components/colorblockTemp.vue";
export default {
  name: "default-text",
  components: {
    richText,
    quoteTemp,
    stateTemp,
    colorblockTemp,
  },
  props: {
    // 类型
    themeType: {
      default: "",
    },
    token: {
      default: "",
    },
    content: {
      default() {
        return {};
      },
    },
    index: {
      default: 0,
    },
    current: {
      default: null,
    },
  },
  provide() {
    return {
      parent: this,
    };
  },
  data() {
    return {};
  },
  mounted() {},
  watch: {},

  methods: {
    onHandler(content) {
      this.$emit("change", content);
    },
    onCommandState(commandState) {
      this.$emit("commandState", commandState);
    },
  },
};
</script>

<style lang="scss">
.default-text {
  padding: 2px 0;
}
.default-text #layEditor-richText {
  outline: none;
  overflow: auto;
  word-break: break-all;
  font-family: system, -apple-system, BlinkMacSystemFont, PingFang SC, Segoe UI,
    Microsoft YaHei, wenquanyi micro hei, Hiragino Sans GB, Hiragino Sans GB W3,
    Roboto, Oxygen, Ubuntu, Cantarell, Fira Sans, Droid Sans, Helvetica Neue,
    Helvetica, Arial, sans-serif;
  line-height: 1.75;
  font-size: 16px;
  -moz-user-modify: read-write;
  -webkit-user-modify: read-write;
}

.default-text #layEditor-richText ul,
.default-text #layEditor-richText ol {
  padding-left: 20px;
}

.default-text #layEditor-richText li {
  list-style-type: "🔸";
  padding-left: 5px;
}
.default-text #layEditor-richText ol li {
  list-style-type: decimal;
}

.default-text #layEditor-richText:empty::before {
  content: attr(placeholder);
  color: #c0c4cf;
}
</style>
